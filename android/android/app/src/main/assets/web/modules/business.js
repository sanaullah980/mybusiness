function esc(v=''){return String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));}
export function renderBusinessCard(container){const s=window.data.settings||{};container.innerHTML=`<div class="business-card-preview"><div class="business-card-brand"><div class="business-card-mark"><i class="fas fa-store"></i></div><div><span>BUSINESS CARD</span><h2>${esc(s.name||'My Business')}</h2></div></div><p>${esc(s.tagline||'Quality service, trusted business')}</p><div class="business-card-contact"><span>${esc(s.phone||'')}</span><span>${esc(s.address||'')}</span></div></div><div class="card"><h3>Business identity</h3><div class="form-group"><label>Business Name</label><input id="bc-name" value="${esc(s.name||'')}"></div><div class="form-group"><label>Tagline</label><input id="bc-tagline" value="${esc(s.tagline||'')}"></div><div class="form-row"><div class="form-group"><label>Phone</label><input id="bc-phone" value="${esc(s.phone||'')}"></div><div class="form-group"><label>Address</label><input id="bc-address" value="${esc(s.address||'')}"></div></div><button class="btn" onclick="saveBusinessCard()">Save Card</button><button class="btn btn-secondary" style="margin-top:8px" onclick="shareBusinessCard()"><i class="fas fa-share-alt"></i> Share</button></div>`;}
export async function saveBusinessCard(){const fields={name:document.getElementById('bc-name').value.trim(),tagline:document.getElementById('bc-tagline').value.trim(),phone:document.getElementById('bc-phone').value.trim(),address:document.getElementById('bc-address').value.trim()};if(!fields.name)return alert('Business name is required.');await window.setDoc(window.doc(window.db,'settings',window.currentUserId),{...fields,ownerId:window.currentUserId},{merge:true});window.data.settings={...window.data.settings,...fields};alert('Business card saved.');renderBusinessCard(document.getElementById('app-content'));}
export async function shareBusinessCard(){const s=window.data.settings||{};const text=`${s.name||'My Business'}\n${s.tagline||''}\n${s.phone||''}\n${s.address||''}`;if(navigator.share)try{await navigator.share({title:s.name||'Business',text});}catch(_){}else await navigator.clipboard?.writeText(text).then(()=>alert('Business card copied.')).catch(()=>alert(text));}
