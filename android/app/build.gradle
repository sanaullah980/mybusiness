plugins {
    id 'com.android.application'
    id 'org.jetbrains.kotlin.android'
}

android {
    namespace 'com.mybusiness.app'
    compileSdk 35

    defaultConfig {
        applicationId 'com.mybusiness.app'
        minSdk 24
        targetSdk 35
        versionCode 1
        versionName '1.0'
    }

    // Both the existing Qwen/llama AAR and the free Whisper AAR publish arm64-v8a
    // native libraries. Restricting the APK to that ABI prevents building an APK
    // that installs on an unsupported ABI and then fails with UnsatisfiedLinkError.
    ndk {
        abiFilters 'arm64-v8a'
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = '17'
    }

    buildTypes {
        release {
            minifyEnabled false
            shrinkResources false
            proguardFiles(
                    getDefaultProguardFile('proguard-android-optimize.txt'),
                    'proguard-rules.pro'
            )
        }
    }

    packaging {
        jniLibs {
            useLegacyPackaging true
        }

        resources {
            excludes += [
                    '/META-INF/{AL2.0,LGPL2.1}'
            ]
        }
    }
}

dependencies {
    implementation 'androidx.core:core-ktx:1.15.0'
    implementation 'androidx.appcompat:appcompat:1.7.0'
    implementation 'androidx.lifecycle:lifecycle-runtime-ktx:2.8.7'
    implementation 'androidx.webkit:webkit:1.12.1'
    implementation 'org.jetbrains.kotlinx:kotlinx-coroutines-android:1.9.0'
    implementation 'dev.ffmpegkit-maintained:llama-android:0.1.1'
    implementation 'dev.ffmpegkit-maintained:whisper-android:1.0.0'
}