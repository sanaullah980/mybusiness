export function renderInventory(container) {
    const data = window.data; const formatCurrency = window.formatCurrency;
    container.innerHTML = `<button class="btn" style="margin-bottom:20px;" onclick="openProductModal()">+ Add Product</button><div class="card" id="product-list">${data.products.length === 0 ? '<p style="color:var(--gray); text-align:center; padding:10px;">No products found.</p>' : data.products.map(p => `<div class="list-item" style="cursor:default;"><div class="list-item-info"><h4>${p.name}</h4><p>Cost: ${formatCurrency(p.cost)} | Price: ${formatCurrency(p.price)}</p><p>Stock: ${p.stock}</p></div><div style="display:flex; flex-direction:column; gap:8px; align-items:flex-end;"><span class="badge ${p.stock <= (p.minStock||5) ? 'badge-low' : 'badge-ok'}">${p.stock <= (p.minStock||5) ? 'Low Stock' : 'OK'}</span><div style="display:flex; gap:5px;"><button class="btn btn-sm btn-secondary" onclick="openStockAdjustModal('${p.id}')">Adjust</button><button class="btn btn-sm" onclick="openProductModal('${p.id}')">Edit</button><button class="btn btn-sm btn-danger" onclick="deleteProduct('${p.id}')">Delete</button></div></div></div>`).join('')}</div>`;
}
export function openProductModal(productId = null) { const p = productId ? window.data.products.find(x => x.id === productId) : null; const modal = document.getElementById('modal-body'); modal.innerHTML = `<div class="modal-header"><h2>${p ? 'Edit' : 'Add'} Product</h2><button class="close-btn" onclick="closeModal()">&times;</button></div><div class="form-group"><label>Product Name *</label><input type="text" id="p-name" value="${p ? p.name : ''}"></div><div class="form-row"><div class="form-group"><label>Cost Price (Rs.) *</label><input type="number" id="p-cost" step="0.01" min="0" value="${p ? p.cost : ''}"></div><div class="form-group"><label>Selling Price (Rs.) *</label><input type="number" id="p-price" step="0.01" min="0" value="${p ? p.price : ''}"></div></div><div class="form-row"><div class="form-group"><label>Current Stock *</label><input type="number" id="p-stock" min="0" value="${p ? p.stock : ''}"></div><div class="form-group"><label>Min Stock Alert</label><input type="number" id="p-min" min="0" value="${p ? (p.minStock||5) : 5}"></div></div><button class="btn" id="btn-save-product" onclick="saveProduct('${productId || ''}')">${p ? 'Update' : 'Save'} Product</button>`; document.getElementById('modal-overlay').classList.remove('hidden'); }
export async function saveProduct(productId) { const name = document.getElementById('p-name').value.trim(); const cost = parseFloat(document.getElementById('p-cost').value); const price = parseFloat(document.getElementById('p-price').value); const stock = parseInt(document.getElementById('p-stock').value); const minStock = parseInt(document.getElementById('p-min').value) || 5; if (!name || isNaN(cost) || isNaN(price) || isNaN(stock)) return alert("Fill all fields."); if (cost < 0 || price < 0 || stock < 0) return alert("No negative values."); window.showLoading('btn-save-product', "Saving..."); try { const pData = { name, cost, price, stock, minStock, ownerId: window.currentUserId }; if (productId) await updateDoc(doc(window.db, "products", productId), pData); else await addDoc(collection(window.db, "products"), pData); alert(productId ? "Updated!" : "Added!"); closeModal(); } catch (error) { console.error(error); alert("Failed."); } finally { window.hideLoading('btn-save-product'); } }
export async function deleteProduct(id) { if (!confirm("Delete?")) return; try { await deleteDoc(doc(window.db, "products", id)); } catch (error) { alert("Failed."); } }
export function openStockAdjustModal(productId) { const p = window.data.products.find(x => x.id === productId); const modal = document.getElementById('modal-body'); modal.innerHTML = `<div class="modal-header"><h2>Adjust Stock: ${p.name}</h2><button class="close-btn" onclick="closeModal()">&times;</button></div><p style="margin-bottom:15px; color:var(--gray);">Current Stock: <strong>${p.stock}</strong></p><div class="form-group"><label>Action</label><select id="adj-type"><option value="add">Add Stock</option><option value="remove">Remove Stock</option></select></div><div class="form-group"><label>Quantity</label><input type="number" id="adj-qty" min="1" value="1"></div><div class="form-group"><label>Note</label><input type="text" id="adj-note"></div><button class="btn" id="btn-save-adj" onclick="saveStockAdjustment('${productId}')">Update Stock</button>`; document.getElementById('modal-overlay').classList.remove('hidden'); }
export async function saveStockAdjustment(productId) { const type = document.getElementById('adj-type').value; const qty = parseInt(document.getElementById('adj-qty').value); const note = document.getElementById('adj-note').value.trim(); if (!qty || qty <= 0) return alert("Valid qty."); const p = window.data.products.find(x => x.id === productId); if (type === 'remove' && p.stock - qty < 0) return alert("Cannot go below zero."); window.showLoading('btn-save-adj', "Updating..."); try { const newStock = type === 'add' ? p.stock + qty : p.stock - qty; await updateDoc(doc(window.db, "products", productId), { stock: newStock }); await addDoc(collection(window.db, "stockAdjustments"), { ownerId: window.currentUserId, productId, type, quantity: qty, date: new Date().toISOString(), note: note || `${type === 'add' ? 'Added' : 'Removed'} stock` }); alert("Updated!"); closeModal(); } catch (error) { alert("Failed."); } finally { window.hideLoading('btn-save-adj'); } }