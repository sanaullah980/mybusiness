export function renderExpenses(container) {
    const data = window.data; const formatCurrency = window.formatCurrency; const getLocalDateStr = window.getLocalDateStr;
    const todayExpenses = data.expenses.filter(e => getLocalDateStr(e.date) === getLocalDateStr(new Date())).reduce((sum, e) => sum + (e.amount || 0), 0);
    container.innerHTML = `<button class="btn" style="margin-bottom:20px;" onclick="openExpenseModal()">+ Add Expense</button><div class="card"><h3>Today's Expenses: ${formatCurrency(todayExpenses)}</h3></div><div class="card" id="expense-list">${data.expenses.length === 0 ? '<p style="color:var(--gray); text-align:center; padding:10px;">No expenses recorded.</p>' : data.expenses.slice().reverse().map(e => `<div class="list-item" style="cursor:default;"><div class="list-item-info"><h4>${e.category || 'Uncategorized'}</h4><p>${new Date(e.date).toLocaleDateString()} ${e.note ? '| ' + e.note : ''}</p></div><div style="display:flex; align-items:center; gap:10px;"><span style="font-weight:bold; color:var(--danger);">${formatCurrency(e.amount)}</span><button class="btn btn-sm btn-danger" onclick="deleteExpense('${e.id}')"><i class="fas fa-trash"></i></button></div></div>`).join('')}</div>`;
}
export function openExpenseModal(expenseId = null) { const e = expenseId ? window.data.expenses.find(x => x.id === expenseId) : null; const modal = document.getElementById('modal-body'); modal.innerHTML = `<div class="modal-header"><h2>${e ? 'Edit' : 'Add'} Expense</h2><button class="close-btn" onclick="closeModal()">&times;</button></div><div class="form-group"><label>Amount (Rs.) *</label><input type="number" id="exp-amount" min="0" step="0.01" value="${e ? e.amount : ''}"></div><div class="form-group"><label>Date *</label><input type="date" id="exp-date" value="${e ? window.getLocalDateStr(e.date) : window.getLocalDateStr(new Date())}"></div><div class="form-group"><label>Category</label><input type="text" id="exp-category" value="${e ? (e.category || '') : ''}"></div><div class="form-group"><label>Note</label><input type="text" id="exp-note" value="${e ? (e.note || '') : ''}"></div><button class="btn" id="btn-save-expense" onclick="saveExpense('${expenseId || ''}')">${e ? 'Update' : 'Save'} Expense</button>`; document.getElementById('modal-overlay').classList.remove('hidden'); }
export async function saveExpense(expenseId) { const amount = parseFloat(document.getElementById('exp-amount').value); const date = document.getElementById('exp-date').value; const category = document.getElementById('exp-category').value.trim(); const note = document.getElementById('exp-note').value.trim(); if (isNaN(amount) || amount <= 0 || !date) return alert("Valid amount and date."); window.showLoading('btn-save-expense', "Saving..."); try { const eData = { amount, date: new Date(date).toISOString(), category, note, ownerId: window.currentUserId }; if (expenseId) await updateDoc(doc(window.db, "expenses", expenseId), eData); else await addDoc(collection(window.db, "expenses"), eData); alert(expenseId ? "Updated!" : "Added!"); closeModal(); } catch (error) { alert("Failed."); } finally { window.hideLoading('btn-save-expense'); } }
export async function deleteExpense(id) { if (!confirm("Delete?")) return; try { await deleteDoc(doc(window.db, "expenses", id)); } catch (error) { alert("Failed."); } }