export function renderSettings(container) {
    const data = window.data; const auth = window.auth;
    const userEmail = auth.currentUser ? auth.currentUser.email : 'Not logged in';
    const isEmailUser = auth.currentUser && auth.currentUser.providerData.some(p => p.providerId === 'password');
    container.innerHTML = `<div class="card"><h3>📱 Mobile App</h3><p style="margin-bottom:15px; font-size:14px; color:var(--gray);">Get the native Android app.</p><a href="/downloads/mybusiness.apk" download class="btn" style="background: var(--dark); text-decoration: none; display: flex; align-items: center; justify-content: center; gap: 8px;"><i class="fab fa-android"></i> Download Android App</a></div><div class="card"><h3>Business Settings</h3><div class="form-group"><label>Business Name</label><input type="text" id="set-name" value="${data.settings.name || ''}"></div><div class="form-group"><label>Currency Symbol</label><input type="text" id="set-currency" value="${data.settings.currency || 'Rs.'}"></div><button class="btn" id="btn-save-settings" onclick="saveSettings()">Save Settings</button></div>${isEmailUser ? `<div class="card"><h3>Account Security</h3><p style="margin-bottom:10px; font-size:14px; color:var(--gray);">Logged in as: ${userEmail}</p><button class="btn" style="background:var(--dark); margin-bottom:10px;" onclick="openChangePasswordModal()">Change Password</button></div>` : `<div class="card"><h3>Account</h3><p style="margin-bottom:10px; font-size:14px; color:var(--gray);">Logged in via Google as: ${userEmail}</p><p style="font-size:12px; color:var(--gray);">Password managed by Google.</p></div>`}`;
}
export async function saveSettings() { const name = document.getElementById('set-name').value.trim(); const currency = document.getElementById('set-currency').value.trim() || 'Rs.'; window.showLoading('btn-save-settings', "Saving..."); try { await setDoc(doc(window.db, "settings", window.currentUserId), { name, currency, ownerId: window.currentUserId }, { merge: true }); window.data.settings = { ...window.data.settings, name, currency }; alert("Saved!"); } catch (error) { alert("Failed."); } finally { window.hideLoading('btn-save-settings'); } }
export function openChangePasswordModal() { const modal = document.getElementById('modal-body'); modal.innerHTML = `<div class="modal-header"><h2>Change Password</h2><button class="close-btn" onclick="closeModal()">&times;</button></div><div class="form-group"><label>Current Password</label><input type="password" id="cp-current"></div><div class="form-group"><label>New Password</label><input type="password" id="cp-new"></div><div class="form-group"><label>Confirm New Password</label><input type="password" id="cp-confirm"></div><button class="btn" id="btn-cp-submit" onclick="changePassword()">Update Password</button>`; document.getElementById('modal-overlay').classList.remove('hidden'); }
export async function changePassword() { const current = document.getElementById('cp-current').value; const newPass = document.getElementById('cp-new').value; const confirm = document.getElementById('cp-confirm').value; if (!current || !newPass || !confirm) return alert("All fields required."); if (newPass.length < 6) return alert("Min 6 characters."); if (newPass !== confirm) return alert("Passwords don't match."); window.showLoading('btn-cp-submit', "Updating..."); try { const user = window.auth.currentUser; const credential = EmailAuthProvider.credential(user.email, current); await reauthenticateWithCredential(user, credential); await updatePassword(user, newPass); alert("Updated!"); closeModal(); } catch (error) { if (error.code === 'auth/wrong-password') alert("Incorrect current password."); else if (error.code === 'auth/requires-recent-login') alert("Please log out and log back in first."); else alert("Failed."); } finally { window.hideLoading('btn-cp-submit'); } }